# Mapa de superfícies

| Funcionalidade | Entidade/tabela | Endpoint | Página ou componente | Teste |
|---|---|---|---|---|
| Senha compartilhada e sessão | Cookie assinado, sem tabela de usuário | `POST /api/session/login`, `GET /api/session`, `POST /api/session/logout` | `/entrar`, `AppShell` | `session-api.test.ts`, `auth.test.ts`, `core.spec.ts` |
| Rebanho, identificação, cadastro em massa e exclusão segura | `animals`, `milk_measurements` | `GET/POST /api/animals`, `POST /api/animals/bulk`, `GET/PATCH/DELETE /api/animals/:id` | `/rebanho` com linha inteira navegável; modos individual/em massa em `/rebanho/novo`; exclusão confirmada no detalhe | `core.spec.ts`, `visual.spec.ts` |
| Ciclo produtivo e histórico de situação | `animals`, `animal_status_events` | `POST /api/animals/:id/status-changes`, `DELETE /api/animals/:id/status-changes/:eventId`, `GET /api/animals/:id` | Seção “Ciclo produtivo e reprodução” no detalhe | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Cio, cobertura, resultado e parto | `animal_reproductive_events` | `POST /api/animals/:id/reproductive-events`, `PATCH/DELETE /api/animals/:id/reproductive-events/:eventId`; parto derivado de mudança para lactação | Formulário contextual, resumo factual e linha do tempo no detalhe do animal | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Lotes e histórico de movimentação | `herd_groups`, `animal_group_assignments` | `GET/POST /api/herd-groups`, `PATCH /api/herd-groups/:id`, `POST /api/animals/:id/group-assignments` | `GroupPicker` contextual no cadastro, retorno à lactação e detalhe; sem página separada | `domain.test.ts`, `core.spec.ts` |
| Aliases exatos | `animal_aliases` | `POST /api/animals/:id/aliases`, `DELETE /api/animal-aliases/:id` | Detalhe do animal | `domain.test.ts`, `core.spec.ts` |
| Sessões e importação de peso | `weight_sessions`, `animal_weights` | `GET/POST /api/weight-sessions`, `POST /api/weight-sessions/validate`, `GET/DELETE /api/weight-sessions/:id`, `PATCH /api/weight-measurements/:id` | `/pesos`, `/pesos/importar`, `/pesos/:id` e histórico no animal | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Produção total diária geral ou por lote | `daily_milk_totals.herd_group_id`, `herd_groups` | `GET/POST /api/daily-milk-totals`, `PATCH/DELETE /api/daily-milk-totals/:id` | seletor “Produção de”, histórico no `DailyMilkPanel` em `/producao`, resumo explícito em `/` | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Coleta do laticínio | `milk_collections` | `GET/POST /api/milk-collections`, `GET/PATCH/DELETE /api/milk-collections/:id` | `/producao/coletas/nova`, `/producao/coletas/:id`, bloco Coletas em `/producao` e resumo em `/` | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Controles individuais de leite e cadastro a partir da revisão | `milk_sessions`, `milk_measurements`, `animals`, históricos de situação/lote | `GET /api/milk-sessions`, `GET/PATCH/DELETE /api/milk-sessions/:id`, `POST /api/milk-sessions/:id/register-unmatched-animals` | `/producao`, `/producao/:id`, `BulkRegisterAnimalsPanel`; `/producao/nova` redireciona para importação | `seed.test.ts`, `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Evolução geral e individual por período | consultas derivadas de `daily_milk_totals`, `milk_sessions`, `milk_measurements` | `GET /api/milk-production-timeline`, `GET /api/animals/:id` | gráfico em `/producao` e no detalhe do animal | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Medições, revisão e estimativa | `milk_measurements` | `PATCH /api/milk-measurements/:id` | `MilkMeasurementEditor` e detalhe do controle | `domain.test.ts`, `seed.test.ts`, `core.spec.ts` |
| Importação e revisão do controle | `milk_sessions`, `milk_measurements`, situação/lote históricos | `POST /api/import/milk-session/validate`, `POST /api/import/milk-session` | `/producao/importar`, filtros de ausentes, duplicadas, períodos, vínculo, confiança e desvio | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Casos de mastite e carência | `mastitis_cases` | `GET/POST /api/mastitis-cases`, `GET/PATCH /api/mastitis-cases/:id` | `/mastite`, `/mastite/nova`, `/mastite/:id`, ficha do animal e Hoje | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Ações de mastite | `mastitis_actions` | `POST /api/mastitis-cases/:id/actions`, `PATCH /api/mastitis-actions/:id` | detalhe do caso e pendências de Hoje | `domain.test.ts`, `core.spec.ts` |
| Fornecedores | `suppliers` | `GET/POST /api/suppliers`, `GET/PATCH /api/suppliers/:id` | `/fornecedores`, `/fornecedores/:id`, compra | `core.spec.ts` cobre o consumidor em compra |
| Compras e vencimentos | `purchases` | `GET/POST /api/purchases`, `GET/PATCH /api/purchases/:id` | `/compras`, `/compras/nova`, `/compras/:id` | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Itens opcionais | `purchase_items` | `POST /api/purchases/:id/items`, `PATCH/DELETE /api/purchase-items/:id` | Detalhe da compra | `domain.test.ts`, `core.spec.ts` |
| Receitas e caixa registrado | `revenues`, `purchases` | `GET/POST /api/revenues`, `GET/PATCH /api/revenues/:id`, `POST /api/revenues/:id/actions`, `GET /api/finance-summary` | `/financeiro`, `/receitas/nova`, `/receitas/:id` | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Preço mensal e estimativa do leite coletado | `monthly_milk_prices`, `milk_collections`, `daily_milk_totals` | `GET /api/milk-prices`, `GET /api/milk-prices/summary`, `PUT /api/milk-prices/:month` | `/financeiro/preco-leite`, acesso no Financeiro e resumo em Hoje | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Saída econômica ou morte | `animal_status_events`, `animal_exits`, vínculo opcional com `revenues` | `POST /api/animals/:id/status-changes`, `GET /api/animals/:id` | “Registrar saída” e histórico no detalhe do animal | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Documentos locais/Drive | `attachments` + provider binário | `GET/POST /api/attachments`, `GET /api/attachments/:id/file`, `PATCH/DELETE /api/attachments/:id` | `/documentos`, detalhe de compra, controle, coleta, receita e saída | `storage.test.ts`, `domain.test.ts`, `core.spec.ts`, checklist Drive real |
| Hoje e visão mensal | consultas derivadas | `GET /api/dashboard` | `/`: pendências clicáveis, quatro ações prioritárias, resumo diário e visão mensal | `core.spec.ts`, `visual.spec.ts` |
| Exportações CSV | consultas de animais, leite, peso, mastite e financeiro | `GET /api/data-exports/:dataset.csv` | `/configuracoes/dados` | `core.spec.ts`, `visual.spec.ts` |
| Backup e restauração | banco PostgreSQL completo | scripts `backup:create` e `backup:restore` | documentação operacional, sem endpoint ou tela destrutiva | ensaio real em banco local descartável |
| Saúde e prontidão | consulta ao PostgreSQL | `GET /api/health`, `GET /api/ready` | Healthcheck Docker/Railway | `session-api.test.ts`, smoke Docker |
| Jogo — mapa do sítio (editor sobre satélite) | `map_zones`, `map_installations` | `GET /api/game/map`, `POST/PATCH/DELETE /api/game/map/zones[/:id]`, `POST/PATCH/DELETE /api/game/map/installations[/:id]` | `/jogo/mapa/editor` (Leaflet em chunk lazy, passo de localização gamificado) | `game-geometry.test.ts`, `game-location.test.ts`, `game.spec.ts` |
| Rebanho por lotes | `herd_groups`, `animal_group_assignments`, `map_zones.herd_group_id` | derivado de `GET /api/animals` + `GET /api/herd-groups` + `GET /api/game/map` | `/rebanho` (cartões de lote com contagem e pasto; busca revela animais), `/rebanho/lote/:groupId` (`sem-lote` para animais sem assignment) | `core.spec.ts`, `visual.spec.ts` |
| Inventário de alimentação (catálogo, compra → estoque → uso) | `feed_items`, `feed_purchase_entries` (crédito vinculado a `purchases`), `feeding_events` + `feeding_event_items` (débito) | `GET/POST/PATCH /api/feed-items`, `GET/POST/DELETE /api/feed-purchase-entries`, `GET/POST/DELETE /api/feeding-events`, `GET /api/feed-inventory` (saldo sempre derivado; uso além do saldo → 409 `BEYOND_BALANCE` com confirmação) | Depósito e Estação de Alimentação em `/jogo`; trato da ordenha na folha da mangueira | `feeding.test.ts`, `game.spec.ts` |
| Trato do dia (tarefa e entrada) | `feeding_events` | `GET /api/dashboard` (`today.feedingCount`, `attention.feedingMissing`) + `POST /api/feeding-events` | tarefa "Trato do dia" em `/`, `/alimentacao/trato/novo`, tipo de entrada "Trato" (`entries.ts`), trato da ordenha na folha da mangueira | `game.spec.ts`, `visual.spec.ts` |
| NL — compra de alimento e trato por voz/texto | `proposed_actions` (`FEED_PURCHASE` cria compra+entry numa transação; `FEEDING_EVENT` cria trato+linhas), `captures` | mesmo pipeline `POST /api/captures` → revisão → `POST /api/captures/:id/actions/:id/commit` (committers em `commit-registry.ts`) | cards próprios em `/revisar` com correção de item/lote/contexto; item ou lote não reconhecido → NEEDS_REVIEW (nunca cria cadastro por inferência) | `nl-feeding.test.ts` |
| Jogo — tabuleiro, rebanho e ordenha pelo mapa | derivado de `map_zones`, `animals`, `animal_group_assignments`, `daily_milk_totals`, `milk_collections`, `monthly_milk_prices`, `purchases` | `GET /api/game/state` (leitura agregada; escritas usam os endpoints reais de produção/coleta) | `/jogo`: mapa SVG estilizado, mangueira com folha de ações, HUD de economia real e streaks | `game-projection.test.ts`, `game-herd-layout.test.ts`, `game-tank.test.ts`, `game-streaks.test.ts`, `game-economy.test.ts`, `game.spec.ts`, `visual.spec.ts` |

Não existe superfície de configuração do Google Drive. Por decisão de escopo, a autorização acontece uma vez com `pnpm google-drive:authorize` e as credenciais ficam nas variáveis do ambiente de produção.
