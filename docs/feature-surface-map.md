# Mapa de superfícies

| Funcionalidade | Entidade/tabela | Endpoint | Página ou componente | Teste |
|---|---|---|---|---|
| Senha compartilhada e sessão | Cookie assinado, sem tabela de usuário | `POST /api/session/login`, `GET /api/session`, `POST /api/session/logout` | `/entrar`, `AppShell` | `session-api.test.ts`, `auth.test.ts`, `core.spec.ts` |
| Rebanho, identificação, cadastro em massa e exclusão segura | `animals`, `milk_measurements` | `GET/POST /api/animals`, `POST /api/animals/bulk`, `GET/PATCH/DELETE /api/animals/:id` | `/rebanho` com linha inteira navegável; modos individual/em massa em `/rebanho/novo`; exclusão confirmada no detalhe | `core.spec.ts`, `visual.spec.ts` |
| Ciclo produtivo e histórico de situação | `animals`, `animal_status_events` | `POST /api/animals/:id/status-changes`, `DELETE /api/animals/:id/status-changes/:eventId`, `GET /api/animals/:id` | Seção “Ciclo produtivo e reprodução” no detalhe | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Cio, cobertura, resultado e parto | `animal_reproductive_events` | `POST /api/animals/:id/reproductive-events`, `PATCH/DELETE /api/animals/:id/reproductive-events/:eventId`; parto derivado de mudança para lactação | Formulário contextual, resumo factual e linha do tempo no detalhe do animal | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Lotes (com ou sem ordenha) e histórico de movimentação | `herd_groups`, `animal_group_assignments` | `GET/POST /api/herd-groups`, `PATCH /api/herd-groups/:id`, `POST /api/animals/:id/group-assignments` | `GroupPicker` contextual no cadastro, retorno à lactação e detalhe; lote como entidade na aba Rebanho do Caderno do jogo (criar/editar/arquivar); sem página separada | `domain.test.ts`, `core.spec.ts` |
| Aliases exatos | `animal_aliases` | `POST /api/animals/:id/aliases`, `DELETE /api/animal-aliases/:id` | Detalhe do animal | `domain.test.ts`, `core.spec.ts` |
| Sessões e importação de peso | `weight_sessions`, `animal_weights` | `GET/POST /api/weight-sessions`, `POST /api/weight-sessions/validate` (usado pela revisão de pesagem por IA na folha da Balança), `GET/DELETE /api/weight-sessions/:id`, `PATCH /api/weight-measurements/:id` | `/pesos`, `/pesos/novo`, `/pesos/:id` e histórico no animal; folha da Balança no jogo (fila vaca a vaca, sessão parcial; intent `WEIGHT_SESSION` revisado no `WeighingReviewFlow` e commitado pela pipeline de capturas) | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Produção total diária geral ou por lote | `daily_milk_totals.herd_group_id`, `herd_groups` | `GET/POST /api/daily-milk-totals`, `PATCH/DELETE /api/daily-milk-totals/:id` | seletor “Produção de”, histórico no `DailyMilkPanel` em `/producao`, resumo explícito na aba Hoje do Caderno | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Coleta do laticínio | `milk_collections` | `GET/POST /api/milk-collections`, `GET/PATCH/DELETE /api/milk-collections/:id` | `/producao/coletas/nova`, `/producao/coletas/:id`, bloco Coletas em `/producao` e resumo na aba Hoje | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Controles individuais de leite e cadastro a partir da revisão | `milk_sessions`, `milk_measurements`, `animals`, históricos de situação/lote | `GET /api/milk-sessions`, `GET/PATCH/DELETE /api/milk-sessions/:id`, `POST /api/milk-sessions/:id/register-unmatched-animals` | `/producao`, `/producao/:id`, `BulkRegisterAnimalsPanel` | `seed.test.ts`, `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Evolução geral e individual por período | consultas derivadas de `daily_milk_totals`, `milk_sessions`, `milk_measurements` | `GET /api/milk-production-timeline`, `GET /api/animals/:id` | gráfico em `/producao` e no detalhe do animal | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Medições, revisão e estimativa | `milk_measurements` | `PATCH /api/milk-measurements/:id` | `MilkMeasurementEditor` e detalhe do controle | `domain.test.ts`, `seed.test.ts`, `core.spec.ts` |
| Importação e revisão do controle | `milk_sessions`, `milk_measurements`, situação/lote históricos | `POST /api/import/milk-session/validate`, `POST /api/import/milk-session` | revisão linha a linha pelo `ImportMilkReview`: na folha da mangueira no jogo (modo revisão da captura de IA) e em `/producao/importar` (fallback), com filtros de ausentes, duplicadas, períodos, vínculo, confiança e desvio | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Casos de mastite e carência | `mastitis_cases` | `GET/POST /api/mastitis-cases`, `GET/PATCH /api/mastitis-cases/:id` | `/mastite`, `/mastite/nova`, `/mastite/:id`, ficha do animal e aba Hoje; folha da Enfermaria no jogo e ação “Registrar mastite” no detalhe do animal do jogo | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Ações de mastite | `mastitis_actions` | `POST /api/mastitis-cases/:id/actions`, `PATCH /api/mastitis-actions/:id` | detalhe do caso e pendências de Hoje | `domain.test.ts`, `core.spec.ts` |
| Fornecedores | `suppliers` | `GET/POST /api/suppliers`, `GET/PATCH /api/suppliers/:id` | área Compras e estoque em `/fornecedores`, `/fornecedores/:id`, compra e revisão OCR; cadastro rápido na folha da Casa no jogo | `core.spec.ts` cobre o consumidor em compra |
| Compras e vencimentos | `purchases` | `GET/POST /api/purchases`, `GET/PATCH /api/purchases/:id` | área Compras e estoque em `/compras`, `/compras/nova`, `/compras/:id`; compra genérica e pendências na folha da Casa no jogo | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Itens opcionais | `purchase_items` | `POST /api/purchases/:id/items`, `PATCH/DELETE /api/purchase-items/:id` | Detalhe da compra | `domain.test.ts`, `core.spec.ts` |
| Receitas e caixa registrado | `revenues`, `purchases` | `GET/POST /api/revenues`, `GET/PATCH /api/revenues/:id`, `POST /api/revenues/:id/actions`, `GET /api/finance-summary` | `/financeiro`, `/receitas/nova`, `/receitas/:id`; receita e “A receber” na folha da Casa no jogo | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Preço mensal e estimativa do leite coletado | `monthly_milk_prices`, `milk_collections`, `daily_milk_totals` | `GET /api/milk-prices`, `GET /api/milk-prices/summary`, `PUT /api/milk-prices/:month` | `/financeiro/preco-leite`, acesso no Financeiro e resumo na aba Hoje; mesmo formulário na folha da Casa no jogo | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Saída econômica ou morte | `animal_status_events`, `animal_exits`, vínculo opcional com `revenues` | `POST /api/animals/:id/status-changes`, `GET /api/animals/:id` | “Registrar saída” e histórico no detalhe do animal | `domain.test.ts`, `core.spec.ts`, `visual.spec.ts` |
| Documentos locais/Railway Bucket | `attachments` + provider binário | `GET/POST /api/attachments`, `GET /api/attachments/:id/file`, `PATCH/DELETE /api/attachments/:id` | `/documentos`, detalhe de compra, controle, coleta, receita e saída; anexo no fato dentro da folha da Casa no jogo | `storage.test.ts`, `domain.test.ts`, `core.spec.ts`, checklist do bucket real |
| Hoje e visão mensal | consultas derivadas | `GET /api/dashboard` | aba Hoje do Caderno do jogo (`TodayPanel`): pendências clicáveis, fechamento do dia, resumo diário e visão mensal | `core.spec.ts`, `visual.spec.ts` |
| Exportações CSV | consultas de animais, leite, peso, mastite e financeiro | `GET /api/data-exports/:dataset.csv` | `/configuracoes/dados` | `core.spec.ts`, `visual.spec.ts` |
| Backup e restauração | banco PostgreSQL completo | scripts `backup:create` e `backup:restore` | documentação operacional, sem endpoint ou tela destrutiva | ensaio real em banco local descartável |
| Saúde e prontidão | consulta ao PostgreSQL | `GET /api/health`, `GET /api/ready` | Healthcheck Docker/Railway | `session-api.test.ts`, smoke Docker |
| Jogo — mapa do sítio (editor sobre satélite) | `map_zones`, `map_installations` | `GET /api/game/map`, `POST/PATCH/DELETE /api/game/map/zones[/:id]`, `POST/PATCH/DELETE /api/game/map/installations[/:id]` | `/jogo/mapa/editor` (Leaflet em chunk lazy, passo de localização gamificado) | `game-geometry.test.ts`, `game-location.test.ts`, `game.spec.ts` |
| Rebanho por lotes | `herd_groups`, `animal_group_assignments`, `pasture_occupancies` | derivado de `GET /api/animals` + `GET /api/herd-groups` + `GET /api/pastures` | `/rebanho` (cartões de lote com contagem e pasto real; busca revela animais), `/rebanho/lote/:groupId` (`sem-lote` para animais vivos sem lotação) | `core.spec.ts`, `visual.spec.ts` |
| Pastos e rotação | `pastures`, `pasture_occupancies` | `GET/POST /api/pastures`, `GET/PATCH /api/pastures/:id`, `GET /api/pastures/:id/occupancies`, `POST /api/pastures/:id/subdivide`, `POST /api/herd-groups/:id/pasture` | `/pastos` virou redirect para o jogo: folha do pasto no tabuleiro (`game-pasture-sheet`) com uso/descanso, rotação com confirmação explícita, renomear, retraçar e subdivisão guiada no editor | `pasture.test.ts`, `core.spec.ts` |
| Inventário de alimentação (catálogo, compra → estoque → uso) | `feed_items`, `feed_purchase_entries` (crédito vinculado a `purchases`), `feeding_events` + `feeding_event_items` (débito) | `GET/POST/PATCH /api/feed-items`, `POST /api/feed-purchases` (compra+crédito atômicos), `GET/POST/DELETE /api/feed-purchase-entries`, `GET/POST/DELETE /api/feeding-events`, `GET /api/feed-inventory` (saldo sempre derivado; unidade bloqueada após a primeira movimentação; uso além do saldo → 409 `BEYOND_BALANCE` com confirmação) | `/estoque-alimentos`, `/catalogo-alimentos`, `/compras/alimentos/nova`; também Depósito e Estação de Alimentação no jogo, com item do catálogo editável/reativável no Depósito e na aba Estoque do Caderno | `feeding.test.ts`, `core.spec.ts`, `game.spec.ts`, `visual.spec.ts` |
| Trato do dia (tarefa e entrada) | `feeding_events` | `GET /api/dashboard` (`today.feedingCount`, `attention.feedingMissing`) + `POST /api/feeding-events` | tarefa "Trato do dia" na aba Hoje do Caderno, `/alimentacao/trato/novo`, tipo de entrada "Trato" (`entries.ts`), trato da ordenha na folha da mangueira | `game.spec.ts`, `visual.spec.ts` |
| NL — compra de alimento e trato por voz/texto/documento | `proposed_actions` (`FEED_PURCHASE` cria/reusa item e fornecedor, cria compra+entry numa transação; `FEEDING_EVENT` cria trato+linhas), `captures` | mesmo pipeline `POST /api/captures` → revisão → `POST /api/captures/:id/actions/:id/commit` (committers em `commit-registry.ts`) | revisão contextual na folha do Depósito (`FeedPurchaseReviewForm`) ou do Cocho no jogo; `/revisar` virou redirect para a aba Pendências do Caderno ("Revisar na folha" no detalhe); documento exige confirmação explícita de quantidade/unidade; cadastro novo só ocorre após decisão humana | `nl-feeding.test.ts` |
| Jogo — tabuleiro, rebanho e ordenha pelo mapa | derivado de `map_zones`, `animals`, `animal_group_assignments`, `daily_milk_totals`, `milk_collections`, `monthly_milk_prices`, `purchases` | `GET /api/game/state` (leitura agregada; escritas usam os endpoints reais de produção/coleta) | `/` (a home; `/jogo` redireciona): mapa SVG estilizado, mangueira com folha de ações, HUD de economia real e streaks | `game-projection.test.ts`, `game-herd-layout.test.ts`, `game-tank.test.ts`, `game-streaks.test.ts`, `game-economy.test.ts`, `game.spec.ts`, `visual.spec.ts` |
| Gráficos e auditoria fora do jogo | consultas derivadas | — | hub `/graficos` (`graficos-hub`): gráficos (produção mensal, evolução de peso, financeiro mensal), documentos, exportação CSV e listas de auditoria; menu mínimo do `AppShell`: Mapa, Caderno (`?caderno=1`), Gráficos | `visual.spec.ts` |

Não existe superfície de configuração do bucket. Por decisão de escopo, as credenciais ficam em referências de variáveis do ambiente de produção e são usadas apenas pelo backend.
